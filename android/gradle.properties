# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx512m -XX:MaxMetaspaceSize=256m
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m

# When configured, Gradle will run in incubating parallel mode.
org.gradle.parallel=true
org.gradle.caching=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Automatically convert third-party libraries to use AndroidX
android.enableJetifier=true

# Enable AAPT2 PNG crunching
android.enablePngCrunchInReleaseBuilds=true

# Strip unused Java/Kotlin code (R8/ProGuard) and unused Android resources from release
# builds - the libraries this app uses (async-storage, svg, print, view-shot) all ship
# their own consumer ProGuard rules, so this is low-risk.
android.enableProguardInReleaseBuilds=true
android.enableShrinkResourcesInReleaseBuilds=true

# Use this property to specify which architecture you want to build.
# You can also override it from the CLI using
# ./gradlew <task> -PreactNativeArchitectures=x86_64
# Real-device-only (arm64-v8a) - drop the emulator's x86_64 too if you need to test there again.
# Actually wired into android/app/build.gradle's ndk.abiFilters - without that it's inert.
reactNativeArchitectures=arm64-v8a

# Use this property to enable support to the new architecture.
# This will allow you to use TurboModules and the Fabric render in
# your application. You should enable this flag either if you want
# to write custom TurboModules/Fabric components OR use libraries that
# are providing them.
newArchEnabled=false

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
# Disabled: the RN 0.74 Gradle plugin's Hermes-bytecode-compile step shells out via
# `cmd /c <hermesc path>` on Windows, which breaks because this project's path
# ("MC Tracker") contains a space - cmd re-parses it and splits at the space. Re-enable
# once the project (or at least Mobile/) lives at a space-free path.
hermesEnabled=false

# Enable GIF support in React Native images (~200 B increase)
enableGifSupport=true
# Enable webp support in React Native images (~85 KB increase)
enableWebpSupport=true
# Enable animated webp support (~3.4 MB increase)
# Disabled by default because iOS doesn't support animated webp
enableWebpAnimatedSupport=false

# Use legacy packaging to compress native libraries in the resulting APK.
useLegacyPackaging=false
